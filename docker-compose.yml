services:
  agentic-lab-api:
    build: .
    container_name: openclaw-mcp-agentic-lab
    env_file:
      - .env
    ports:
      - "8080:8080"
    restart: unless-stopped

  # Optional: uncomment if you want local OSS model serving in Docker.
  # ollama:
  #   image: ollama/ollama:latest
  #   ports:
  #     - "11434:11434"
  #   volumes:
  #     - ollama:/root/.ollama

# volumes:
#   ollama:
